# Docker Setup

This directory contains Docker configuration files for MongoDB.

## Files

- `mongo-init/01-init-db.js`: MongoDB initialization script that creates the database, collections, and indexes

## Usage

See `../DOCKER_SETUP.md` for complete setup instructions.

Quick start:
```bash
docker-compose up -d
```

